# support/
> L2 | 父级: /codex.md

成员清单
Info.plist: macOS app bundle 元数据模板，定义可执行文件、bundle id、图标、版本号与 LSUIElement 后台菜单栏模式

模块法则:
support 只保存打包模板，不承载运行时逻辑。行为发布必须同步版本号，避免安装验证读到旧身份。

[PROTOCOL]: 变更时更新此头部，然后检查 codex.md
